# Customer

## 描述

前端用戶，可為已登入的 WordPress 用戶或未登入的訪客。透過合約模板前端頁面填寫欄位、簽名並送出合約。已登入用戶的 `user_id` 會記錄在合約中，訪客的 `post_author` 為 0。

## 關鍵屬性

- 可瀏覽已發佈的合約模板頁面（`contract_template` 單頁）
- 可填寫合約欄位（姓名、地址、身分證、電話、金額等）
- 可使用簽名板簽名
- 送出合約後可看到簽署完成 Modal
- 已登入用戶可在 WooCommerce 我的帳號 > 訂單詳情中查看合約
- 訪客（未登入用戶）亦可簽署合約
