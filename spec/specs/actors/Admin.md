# Admin

## 描述

WordPress 管理員（角色為 `administrator`），擁有 `manage_options` 與 `edit_post` 權限。負責建立合約模板、上傳公司章、設定外掛參數、審核客戶簽署的合約（核准或拒絕）、批量管理合約狀態。

## 關鍵屬性

- 擁有 WordPress 後台存取權限
- 可建立/編輯合約模板（`contract_template` CPT）
- 可查看/審核合約（`contract` CPT）
- 可存取外掛設定頁面（`manage_options`）
- 可在 WooCommerce 訂單列表查看關聯合約
